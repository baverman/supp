# Current
* Goto location and autocomplete should use different strategies. Location should stop on final
  name with location. Autocomplete should resolve actual type. Currently it's mixed up and fails
  miserably. Maybe a new design is needed. Or two step evaluation: get object representing point in ast
  and get object type. First one is enough to get location.

# Annotations
* ? multiple locations


# Misc
* usage graph
* imports checker
* hints to ignore lint errors/warnings


# Cases
